// ─── Demo plates for the POC simulation buttons ─────────────────
// Only plates registered in the Vehicle table (access control)
// QTF6E90 is only in VehicleLookup (DETRAN) — use PlateCapture to register it first
export const DEMO_PLATES = ['BRA2O26', 'ABC3D45', 'XYZ1234', 'BLQ9A87', 'PEN5B23'] as const;

// ─── Viewfinder layout constants ─────────────────────────────────
export const VIEWFINDER_CAMERA_HEIGHT = 200;
export const VIEWFINDER_QR_HEIGHT = 240;
export const BRACKET_THICKNESS = 3;
export const BRACKET_COLOR = '#FF6B35';
